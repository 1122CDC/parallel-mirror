# 当前计划

更新时间：2026-03-30 00:07

## 当前状态
- H5 的项目品牌已经统一到 `Hello, my world`。
- 未登录用户已经会被强制带到登录页。
- 登录页已经改成登录 / 注册二合一入口。
- 旧版单文件原型 `WMH1.html` 里的旧品牌标识也已经开始清理。
- 目前外网的 `trycloudflare` 临时地址不可用，因为 `cloudflared` 已经退出，`127.0.0.1:3001` 也没有在监听。
- 本地仓库已经初始化成 Git 仓库，`.gitignore` 也已经补上，但还没有接到 GitHub 远端。
- GitHub 仓库 `https://github.com/1122CDC/parallel-mirror` 已经保留原项目，并新增了 `WHM1.0/` 这个子目录。

## 当前目标
- 把所有用户可见的品牌文案统一成 `Hello, my world`。
- 保持“没有登录就不能进入主界面”的规则。
- 不破坏现有的账号隔离、空白新世界和分支独立逻辑。
- 让本机 `3001` 服务和外网临时地址重新可访问。
- 把代码同步到 GitHub，方便后续跨电脑继续改。
- 保持 GitHub 仓库里原项目和 `WHM1.0/` 两套内容都能共存。

## 下一步
1. 如果你愿意，我可以继续把 `WHM1.0/` 里的部署方案理顺，看看是继续修 `cloudflared`，还是改成更稳的正式部署。
2. 如果你想继续在 GitHub 仓库里改，我也可以帮你直接在这个仓库里接着整理。
